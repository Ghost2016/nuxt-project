import Vue from 'vue'
// import Element from 'element-ui'
import locale from 'element-ui/lib/locale/lang/en'

import { Button, Form, FormItem, Dialog, Input, Col, Row, Select, Option, Radio, RadioGroup} from 'element-ui'

Vue.use(Button, { locale })
Vue.use(Form, { locale })
Vue.use(FormItem, { locale })
Vue.use(Dialog, { locale })
Vue.use(Input, { locale })
Vue.use(Col, { locale })
Vue.use(Row, { locale })
Vue.use(Option, { locale })
Vue.use(Select, { locale })
Vue.use(Radio, { locale })
Vue.use(RadioGroup, { locale })
